import 'package:flutter/material.dart';

class ComposerScreen extends StatelessWidget {
  const ComposerScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Composer Screen'),
      ),
    );
  }
}
